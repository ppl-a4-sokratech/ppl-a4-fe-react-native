# Backend base URL for native (Android/iOS) and local web dev.
# On Vercel, set this to "/api/proxy" so requests go through vercel.json rewrites
# (avoids HTTPS-to-HTTP mixed content). The actual backend origin lives in vercel.json.
EXPO_PUBLIC_SOKRATECH_API_DOMAIN=http://localhost:3000
